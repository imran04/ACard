@model Offers.Core.Deal

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Deal</legend>

    <div class="display-label">DealName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DealName)
    </div>

    <div class="display-label">ShortDesc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ShortDesc)
    </div>

    <div class="display-label">FullDesc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.FullDesc)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
